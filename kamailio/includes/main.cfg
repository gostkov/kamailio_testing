request_route {
    route(OPTIONS_CHECK);
    route(NATMANAGE);
    route(AUTH);
    route(REGISTRAR);
    if (!route(LOCATION)) {
        xlog("L_INFO", "Lookup was failed");
        exit;
    }
    route(RELAY);
    exit;
}
